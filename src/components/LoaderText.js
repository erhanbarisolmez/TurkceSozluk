import Box from './box';

export default function LoaderText({widthSize,heightSize, ...props}) {
  return (
    <Box {...props} bg='light' width={widthSize} height={heightSize}>
    
    </Box>
  );
}
 